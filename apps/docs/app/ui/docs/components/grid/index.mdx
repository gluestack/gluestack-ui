import { CodePreviewer } from '@/components/code-previewer';
import { Grid, GridItem } from '@/components/ui/grid';
import { Text } from '@/components/ui/text';

import { Tabs, TabItem } from "@/docs-components/tabs";
import {
  Table,
  TableHeader,
  TableCell,
  TableHeaderCell,
  TableBody,
  TableRow,
} from "@/docs-components/table";
import { InlineCode } from "@/docs-components/inline-code";

# Grid

Discover a powerful Grid component for React & React Native with customizable layout and behavior. Perfect for creating responsive grid layouts in your UI design.

### This is an illustration of Grid component.
<CodePreviewer
  code={`function Example() {
  return (
    <Grid
      className="gap-5"
      _extra=\\{{className: "grid-cols-8"}}
    >
      <GridItem
        className="bg-background-50 p-6 rounded-md"
        _extra=\\{{className: "col-span-3"}}
      />
      <GridItem
        className="bg-background-50 p-6 rounded-md"
        _extra=\\{{className: "col-span-5"}}
      />
      <GridItem
        className="bg-background-50 p-6 rounded-md"
        _extra=\\{{className: "col-span-6"}}
      />
      <GridItem
        className="bg-background-50 p-6 rounded-md"
        _extra=\\{{className: "col-span-4"}}
      />
      <GridItem
        className="bg-background-50 p-6 rounded-md"
        _extra=\\{{className: "col-span-4"}}
      />
    </Grid>
  )
}`}
  argTypes={{}}
  reactLive={{ Grid, GridItem }}
/>

<br />
## Installation

<Tabs>
<TabItem label="CLI">
### Run the following command:
  ```bash
  npx gluestack-ui add grid
  ```
</TabItem>
<TabItem label="Manual">
### Step 1: Copy and paste the following code into your project.

```jsx 
<!-- Failed to load file: core-components/nativewind/grid/index.tsx  --> 
```

> Note: **Step 2 is optional and only required if you want to add support for [React Server Components](https://vercel.com/blog/understanding-react-server-components), You can skip this and jump to Step 3 directly if you don't have this requirement.**

### Step 2(optional): Copy and paste the following code into your project.

```jsx 
<!-- Failed to load file: core-components/nativewind/grid/index.web.tsx  --> 
```

### Step 3: Copy and paste the following code into your project.

```jsx 
<!-- Failed to load file: core-components/nativewind/grid/styles.tsx  --> 
```

### Step 4: Update the import paths to match your project setup.
</TabItem>
</Tabs>

## API Reference

To use this component in your project, include the following import statement in your file.

```ts
import { Grid, GridItem } from '@/components/ui/grid';
```

```ts
export default () => (
  <Grid>
    <GridItem />
  </Grid>
);
```

> Note: Our responsive grid component is based on a 12-column grid layout. It follows the CSS **grid system** on the web and **flexbox layout** on native devices. Since the grid layout is only supported on the web, passing the grid-cols and col-span classNames inside _extra is **recommended** for the grid component to work effortlessly on both web and native. Our grid component currently does not support rowSpan.

> Note: The immediate parent of **GridItem** must be **Grid**. There should be no higher-order component (HOC) between them.

### Component Props

This section provides a comprehensive reference list for the component props, detailing descriptions, properties, types, and default behavior for easy project integration.

### Grid

<p>{`Renders a <div /> on web and a View on native.`}</p>

<>
<Table>
<TableBody>
  <TableRow>
    <TableHeaderCell>Platform</TableHeaderCell>
    <TableHeaderCell>Output</TableHeaderCell>
  </TableRow>
  <TableRow>
    <TableCell><InlineCode>Web</InlineCode></TableCell>
    <TableCell>{`<div />`}</TableCell>
  </TableRow>
  <TableRow>
    <TableCell><InlineCode>Native</InlineCode></TableCell>
    <TableCell>{`<View />`}</TableCell>
  </TableRow>
</TableBody>
</Table>
</>

#### Props

<>
<Table>
<TableBody>
  <TableRow>
    <TableHeaderCell>Prop</TableHeaderCell>
    <TableHeaderCell>Type</TableHeaderCell>
    <TableHeaderCell>Default</TableHeaderCell>
    <TableHeaderCell>Description</TableHeaderCell>
  </TableRow>
  <TableRow>
    <TableCell><InlineCode>_extra</InlineCode></TableCell>
    <TableCell>object</TableCell>
    <TableCell>-</TableCell>
    <TableCell>{`Accepts grid-cols-* className. Value for * can range from 1 to 12. Default value is grid-cols-12.`}</TableCell>
  </TableRow>
</TableBody>
</Table>
</>

### GridItem

<p>{`Renders a <div /> on web and a View on native.`}</p>

<>
<Table>
<TableBody>
  <TableRow>
    <TableHeaderCell>Platform</TableHeaderCell>
    <TableHeaderCell>Output</TableHeaderCell>
  </TableRow>
  <TableRow>
    <TableCell><InlineCode>Web</InlineCode></TableCell>
    <TableCell>{`<div />`}</TableCell>
  </TableRow>
  <TableRow>
    <TableCell><InlineCode>Native</InlineCode></TableCell>
    <TableCell>{`<View />`}</TableCell>
  </TableRow>
</TableBody>
</Table>
</>

#### Props

<>
<Table>
<TableBody>
  <TableRow>
    <TableHeaderCell>Prop</TableHeaderCell>
    <TableHeaderCell>Type</TableHeaderCell>
    <TableHeaderCell>Default</TableHeaderCell>
    <TableHeaderCell>Description</TableHeaderCell>
  </TableRow>
  <TableRow>
    <TableCell><InlineCode>_extra</InlineCode></TableCell>
    <TableCell>object</TableCell>
    <TableCell>-</TableCell>
    <TableCell>{`Accepts col-span-* className. Value for * can range from 1 to 12. Default value is col-span-1.`}</TableCell>
  </TableRow>
</TableBody>
</Table>
</>

### Examples

The Examples section provides visual representations of the different variants of the component, allowing you to quickly and easily determine which one best fits your needs. Simply copy the code and integrate it into your project.

### Examples

The Examples section provides visual representations of the different variants of the component, allowing you to quickly and easily determine which one best fits your needs. Simply copy the code and integrate it into your project.

#### Setting the gap between grid items

Use the gap-* utilities to change the gap between both rows and columns in grid.

<CodePreviewer
  code={`function Example() {
  return (
    <Grid className="gap-4" _extra=\\{{
      className: 'grid-cols-9',
    }}>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-3',
        }}
      >
        <Text>A</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-3',
        }}
      >
        <Text>B</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-3',
        }}
      >
        <Text>C</Text>
      </GridItem>
    </Grid>
  );
}`}
  argTypes={{}}
  reactLive={{ Grid, GridItem, Text }}
/>

#### Changing row and column gaps independently

Use the gap-x-* and gap-y-* utilities to change the gap between columns and rows independently.

<CodePreviewer
  code={`function Example() {
  return (
    <Grid className="gap-y-2 gap-x-4" _extra=\\{{
      className: 'grid-cols-6',
    }}>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-2',
        }}
      >
        <Text className="text-sm">01</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-2',
        }}
      >
        <Text className="text-sm">02</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-2',
        }}
      >
        <Text className="text-sm">03</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-2',
        }}
      >
        <Text className="text-sm">04</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-2',
        }}
      >
        <Text className="text-sm">05</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-2',
        }}
      >
        <Text className="text-sm">06</Text>
      </GridItem>
    </Grid>
  );
}`}
  argTypes={{}}
  reactLive={{ Grid, GridItem, Text }}
/>

#### Nested Grids

The following example depicts how easily you can nest grids to create complex layouts with multiple levels of nesting.

<CodePreviewer
  code={`function Example() {
  return (
    <Grid className="gap-3" _extra=\\{{
      className: 'grid-cols-8',
    }}>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-4',
        }}
      >
        <Text className="text-sm">01</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-4',
        }}
      >
        <Text className="text-sm">02</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-4',
        }}
      >
        <Grid className="gap-5" _extra=\\{{
          className: 'grid-cols-2',
        }}>
          <GridItem className="bg-background-200 p-2 rounded-md" _extra=\\{{
            className: 'col-span-1',
          }}><Text className="text-sm">1</Text></GridItem>
          <GridItem className="bg-background-200 p-2 rounded-md" _extra=\\{{
            className: 'col-span-1',
          }}><Text className="text-sm">2</Text></GridItem>
          <GridItem className="bg-background-200 p-2 rounded-md" _extra=\\{{
            className: 'col-span-1',
          }}><Text className="text-sm">3</Text></GridItem>
          <GridItem className="bg-background-200 p-2 rounded-md" _extra=\\{{
            className: 'col-span-1',
          }}><Text className="text-sm">4</Text></GridItem>
        </Grid>
      </GridItem>
      <GridItem
        className="bg-background-50 p-3 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-4',
        }}
      > 
        <Grid className="gap-5" _extra=\\{{
          className: 'grid-cols-4',
        }}>
          <GridItem className="bg-background-200 p-2 rounded-md" _extra=\\{{
            className: 'col-span-4',
          }}><Text className="text-sm">1</Text></GridItem>
          <GridItem className="bg-background-200 p-2 rounded-md" _extra=\\{{
            className: 'col-span-4',
          }}><Text className="text-sm">2</Text></GridItem>
        </Grid>
      </GridItem>
    </Grid>
  );
}`}
  argTypes={{}}
  reactLive={{ Grid, GridItem, Text }}
/>

#### Responsive Grids

You can pass different grid-cols-* and col-span-* values at different breakpoints to create a more responsive layout.

<CodePreviewer
  code={`function Example() {
  return (
    <Grid className="gap-y-2 gap-x-4" _extra=\\{{
      className: 'grid-cols-6',
    }}>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-6 md:col-span-3 lg:col-span-2',
        }}
      >
        <Text className="text-sm">01</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-6 md:col-span-3 lg:col-span-2',
        }}
      >
        <Text className="text-sm">02</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-6 md:col-span-3 lg:col-span-2',
        }}
      >
        <Text className="text-sm">03</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-6 md:col-span-3 lg:col-span-2',
        }}
      >
        <Text className="text-sm">04</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-6 md:col-span-3 lg:col-span-2',
        }}
      >
        <Text className="text-sm">05</Text>
      </GridItem>
      <GridItem
        className="bg-background-50 p-4 rounded-md text-center"
        _extra=\\{{
          className: 'col-span-6 md:col-span-3 lg:col-span-2',
        }}
      >
        <Text className="text-sm">06</Text>
      </GridItem>
    </Grid>
  );
}`}
  argTypes={{}}
  reactLive={{ Grid, GridItem, Text }}
/>