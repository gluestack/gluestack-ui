import type { ComponentMeta } from '@storybook/react-native';
import Divider from './Divider';

const DividerMeta: ComponentMeta<typeof Divider> = {
  title: 'unstyled/stories/DATA DISPLAY/Divider',
  component: Divider,
};

export default DividerMeta;

export { Divider };
