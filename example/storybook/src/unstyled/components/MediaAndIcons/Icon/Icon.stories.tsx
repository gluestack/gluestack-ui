import type { ComponentMeta } from '@storybook/react-native';
import IconStory from './Icon';

const IconMeta: ComponentMeta<any> = {
  title: 'unstyled/stories/MEDIA AND ICONS/Icons',
  component: IconStory,
  argTypes: {},
  args: {},
};

export default IconMeta;
export { IconStory };
