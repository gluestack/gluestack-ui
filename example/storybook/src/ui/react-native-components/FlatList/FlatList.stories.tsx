import type { ComponentMeta } from '@storybook/react-native';
import FlatList from './FlatList';

const FlatListMeta: ComponentMeta<typeof FlatList> = {
  title: 'ui/stories/React Native Components/FlatList',
  component: FlatList,
};

export default FlatListMeta;

export { FlatList };
