import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="components/HStack" />

import { AccessibleHStack } from './HStack';
import {
  AppProvider,
  CodePreview,
  Table,
  Text,
} from '@gluestack/design-system';
import { View } from 'react-native';
import { Wrapper } from '../Wrapper';

# HStack

A `HStack` component is used to create a horizontal stack layout for child components.

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
        return (<Wrapper><HStack
        space="md"
        sx={{ justifyContent: 'center', alignItems: 'center' }}
      >
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
      </HStack></Wrapper>)
    }`,
      scope: { HStack: AccessibleHStack, View, Wrapper },
      argsType: {},
    }}
  />
</AppProvider>

<br />

## Features

- Helpful in layouting components in a horizontal manner and also helps in giving equal space between the components.

## Installation

```jsx
yarn add @universa11y/hstack
```

## Usage

```jsx
import { createHStack } from '@universa11y/hstack';
const HStack = createHStack({
  Root,
  Spacer,
});
```

These are the styled components that we need to pass in the creator API:

### Root

```jsx
const Root = styled(
  View,
  {
    ...
  },
);
```

### Root

```jsx
const Spacer = styled(
  View,
  {
    ...
  },
);
```

## Anatomy

```jsx
export default () => (
  <HStack>
    <View />
    <View />
    <View />
  </HStack>
);
```

## API Reference

### Root

Contains all View related layout style props and actions.
It inherits all the properties of React Native's [View](https://reactnative.dev/docs/view#props) component.

<AppProvider>
  <Table>
    <Table.THead sx={{ _dark: { bg: '$gray800' }, _light: { bg: '$gray100' } }}>
      <Table.TR>
        <Table.TH>
          <Text>Prop</Text>
        </Table.TH>
        <Table.TH>
          <Text>Type</Text>
        </Table.TH>
        <Table.TH>
          <Text>Default</Text>
        </Table.TH>
        <Table.TH>
          <Text>Description</Text>
        </Table.TH>
      </Table.TR>
    </Table.THead>
    <Table.TBody>
      <Table.TR>
        <Table.TD>
          <Text>space</Text>
        </Table.TD>
        <Table.TD>
          <Text>number / string / undefined</Text>
        </Table.TD>
        <Table.TD>
          <Text></Text>
        </Table.TD>
        <Table.TD>
          <Text>It sets the space between children</Text>
        </Table.TD>
      </Table.TR>
    </Table.TBody>
  </Table>
</AppProvider>

## Example

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: `
      function App () {
       return (<Wrapper><HStack
        space="md"
        sx={{ justifyContent: 'center', alignItems: 'center' }}
      >
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
        <View
          style={{ height: 20, width: 20, backgroundColor: 'red' }}
        />
      </HStack></Wrapper>)
    }`,
      scope: { HStack: AccessibleHStack, View, Wrapper },
      argsType: {},
    }}
  />
</AppProvider>
