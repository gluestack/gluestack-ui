import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="components/Divider" />

import { AccessibleDivider } from './Divider';
import { AppProvider, CodePreview } from '@gluestack/design-system';
import { View } from 'react-native';

# Divider

A Divider component is used to create a visual separation between different sections or elements in a layout.

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
        return <View style={{width:100}}><Divider/></View>
    }`,
      scope: { Divider: AccessibleDivider, View },
      argsType: {},
    }}
  />
</AppProvider>

<br />

## Features

- Can create variants of the Divider component by using the `variant` or `orientation` prop.

## Installation

```jsx
yarn add @universa11y/divider
```

## Usage

```jsx
import { createDivider } from '@universa11y/divider';
const Divider = createDivider({
  Root,
});
```

These are the styled components that we need to pass in the creator API:

### Root

```jsx
const Root = styled(
  View,
  {
    ...
  },
);
```

## Anatomy

```jsx
export default () => <Divider />;
```

## API Reference

### Root

Contains all View related layout style props and actions.
It inherits all the properties of React Native's [View](https://reactnative.dev/docs/view#props) component.

| Prop        | Type                | Default    | Description                      |
| ----------- | ------------------- | ---------- | -------------------------------- |
| orientation | horizontal/vertical | horizontal | It sets the direction of divider |

## Example

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
       return <View style={{width:100}}><Divider/></View>
    }`,
      scope: { Divider: AccessibleDivider, View },
      argsType: {},
    }}
  />
</AppProvider>

## Accessibility

It sets the aria-orientation attribute to the divider.
