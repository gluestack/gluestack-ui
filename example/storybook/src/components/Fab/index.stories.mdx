import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="components/Fab" />

import { AccessibleFab, HamburgerIcon } from './Fab';
import { AppProvider, CodePreview } from '@gluestack/design-system';
import { View } from 'react-native';
import { Wrapper } from '../Wrapper';

# Fab

A `floating action button (FAB)` is a circular icon button that hovers over content to execute a primary action in the application.

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
        return (<Wrapper>
         <Fab
        variant={'bottom-right'}
        sx={{ top: 20, right: 20, alignSelf: 'center', h: 40 }}
      >
        <HamburgerIcon sx={{ w: 20, h: 20 }} color="white" />
        <Fab.Label>Menu</Fab.Label>
      </Fab>></Wrapper>)
    }`,
      scope: { Fab: AccessibleFab, Wrapper, HamburgerIcon },
      argsType: {},
    }}
  />
</AppProvider>

<br />

## Installation

```jsx
yarn add @universa11y/fab
```

## Usage

```jsx
import { createFab } from '@universa11y/fab';
const Fab = createFab({
  Root,
  Label,
});
```

These are the styled components that we need to pass in the creator API:

### Root

```jsx
const Root = styled(
  Pressable,
  {
    ...
  },
);
```

### Label

```jsx
const Label = styled(
  Text,
  {
    ...
  },
);
```

## Anatomy

```jsx
export default () => (
  <Fab>
    <Icon />
    <Fab.Label></Fab.Label>
  </Fab>
);
```

## API Reference

### Fab

It inherits all the properties of React Native's [Pressable](https://reactnative.dev/docs/pressable#props) component.

### Fab.Label

It also inherits all the properties of React Native's [Text](https://reactnative.dev/docs/text#props) component.

## Example

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
        return (<Wrapper>
         <Fab
        variant={'bottom-right'}
        sx={{ top: 20, right: 20, alignSelf: 'center', h: 40 }}
      >
        <HamburgerIcon sx={{ w: 20, h: 20 }} color="white" />
        <Fab.Label>Menu</Fab.Label>
      </Fab>></Wrapper>)
    }`,
      scope: { Fab: AccessibleFab, Wrapper, HamburgerIcon },
      argsType: {},
    }}
  />
</AppProvider>
