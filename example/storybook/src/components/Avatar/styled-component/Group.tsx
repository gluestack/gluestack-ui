import { View } from 'react-native';
import { styled } from '@dank-style/react';

export default styled(
  View,
  {
    baseStyle: {
      style: {
        flexDirection: 'row',
        position: 'relative',
      },
    },
  },
  {}
);
