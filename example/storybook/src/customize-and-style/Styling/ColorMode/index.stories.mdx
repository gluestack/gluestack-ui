---
title: Color Modes | gluestack-ui

description: gluestack-ui provides an easy way to manage color modes, in this guide we will learn how we can change and manage color modes.

pageTitle: Color Modes

pageDescription: gluestack-ui provides an easy way to manage color modes, in this guide we will learn how we can change and manage color modes.

showHeader: true
---

import { Meta } from '@storybook/addon-docs';

<Meta title="Customize and Style/Styling/Color Mode" />

## Color Modes

We can apply different styles based on different color mode by defining \_dark and \_light property in sx prop.

In the below example we are defining styles to be applied in dark mode in \_dark object and for light mode in \_light object.

```jsx
import { config } from './gluestack-ui.config';
import { Box, Button, GluestackUIProvider } from '@gluestack-ui/react';
import { useState } from 'react';

export default function App() {
  const [theme, setTheme] = useState('light');
  return (
    <GluestackUIProvider config={config.theme} colorMode={theme}>
      <Box
        justifyContent="center"
        alignItems="center"
        h="100%"
        sx={{
          _dark: {
            bg: '$light900',
          },
          _light: {
            bg: '$light200',
          },
        }}
      >
        <Button
          sx={{
            _dark: {
              bg: '$red400',
            },
            _light: {
              bg: '$red600',
            },
          }}
        >
          <Button.Text>Hello world</Button.Text>
        </Button>
        <Button
          onPress={() => {
            setTheme(theme == 'light' ? 'dark' : 'light');
          }}
        >
          <Button.Text>Toggle Theme</Button.Text>
        </Button>
      </Box>
    </GluestackUIProvider>
  );
}
```

[Expo snack](https://snack.expo.dev/@gluestack/gluestack-ui-color-mode) link for managing color mode.
