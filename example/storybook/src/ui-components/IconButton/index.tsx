import { createIconButton } from '@gluestack-ui/icon-button';
import { Root, Text, Spinner } from './styled-components';

export const IconButton = createIconButton({
  Root,
  Text,
  Spinner,
}) as any;
