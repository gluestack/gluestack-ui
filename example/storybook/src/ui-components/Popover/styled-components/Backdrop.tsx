import { Pressable } from 'react-native';
import { styled } from '../../styled';

export default styled(
  Pressable,
  {
    position: 'absolute',
    left: 0,
    top: 0,
    opacity: 0.1,
    right: 0,
    bottom: 0,
  },
  {}
);
