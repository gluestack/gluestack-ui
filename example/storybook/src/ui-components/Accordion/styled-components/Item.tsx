// @ts-nocheck
import { styled } from '../../styled';
import { View } from 'react-native';

export default styled(
  View,
  {
    h: 60,
    w: 900,
  },
  {
    descendantStyle: [],
    ancestorStyle: [],
  }
);
