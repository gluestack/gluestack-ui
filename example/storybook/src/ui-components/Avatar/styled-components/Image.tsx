import { Image } from 'react-native';
import { styled } from '../../core/styled';

export default styled(
  Image,
  {
    w: '100%',
    h: '100%',
    borderRadius: 9999,
    position: 'absolute',
  },
  {}
);
