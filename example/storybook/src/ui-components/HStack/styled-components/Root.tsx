import { styled } from '@gluestack-ui/styled';
import { View } from 'react-native';

export default styled(
  View,
  {
    flexDirection: 'row',

    defaultProps: {
      space: 'md',
    },
  },
  {}
);
