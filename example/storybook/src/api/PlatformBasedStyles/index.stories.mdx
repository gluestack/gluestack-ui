---
title: Platform Based Styles | Dank
description: Writing platform based styles in Dank. This allows you to easily create styles that are specific to a particular platform that is, Web, iOS, or Android.
---

import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="api/Platform Based Styles" />

import { styled, StyledProvider } from '@dank-style/react';
import { Pressable } from 'react-native';
import { Button, AppProvider, CodePreview } from '@gluestack/design-system';
import { config } from './config';

# Platform Based Styles

This allows you to easily create styles that are specific to a particular platform that is, **Web**, **iOS**, or **Android**.

<AppProvider>
  <CodePreview
    showArgsController={false}
    metaData={{
      scope: { styled, Pressable, Provider: StyledProvider, config },
      code: `
          function App () {
          const StyledButton = styled(Pressable, {
            bg: '$primary600',
            px: '$6',
            py: '$4',
            _web: {
              bg: '$amber500',
            },
            _ios: {
              bg: '$blue500',
            },
            _android: {
              bg: '$red500',
            },
          });
          return (
            <Provider config={config}>
              <StyledButton />
            </Provider>
          )
        }
      `,
      argsType: {
        bg: {
          control: 'input',
          default: '$primary600',
        },
      },
    }}
  />
</AppProvider>

```jsx
const StyledButton = styled(Pressable, {
  bg: '$primary600',
  px: '$6',
  py: '$4',
  _web: {
    bg: '$amber500',
  },
  _ios: {
    bg: '$blue500',
  },
  _android: {
    bg: '$red500',
  },
});
```

Although we provide this feature we do not recommend using it extensively

It is worth noting that, while this approach allows for a more customized experience on each platform, it can also make the codebase more complex and harder to maintain, as it requires additional logic to handle the different props for different platforms.
Therefore, it is important to strike a balance between providing a customized experience and keeping the codebase maintainable.
