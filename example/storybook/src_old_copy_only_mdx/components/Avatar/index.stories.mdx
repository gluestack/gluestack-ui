import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="components/Avatar" />

import { AccessibleAvatar } from './Avatar';
import { AppProvider, CodePreview } from '@gluestack/design-system';
import { View } from 'react-native';
import { Wrapper } from '../Wrapper';

# Avatar

The `Avatar` component can display profile pictures, initials, or a fallback icon to represent a user.

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
        return (<Wrapper>
          <Avatar.Group>
        <Avatar size="md">
          <Avatar.FallbackText>AB</Avatar.FallbackText>
          <Avatar.Image
            source={{
              uri: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=687&q=80',
            }}
          />
          <Avatar.Badge />
        </Avatar>
      </Avatar.Group></Wrapper>)
    }`,
      scope: { Avatar: AccessibleAvatar, Wrapper },
      argsType: {},
    }}
  />
</AppProvider>

<br />

## Features

- Returns a fallback to initials if given

## Installation

```jsx
yarn add @universa11y/avatar
```

## Usage

```jsx
import { createAvatar } from '@universa11y/avatar';
const Avatar = createAvatar({
  Root,
  Badge,
  Group,
  Image,
  FallbackText,
});
```

These are the styled components that we need to pass in the creator API:

### Root

```jsx
const Root = styled(
  View,
  {
    ...
  },
);
```

### Badge

```jsx
const Badge = styled(
  View,
  {
    ...
  },
);
```

### Group

```jsx
const Group = styled(
  View,
  {
    ...
  },
);
```

### Image

```jsx
const Image = styled(
  Image,
  {
    ...
  },
);
```

### FallbackText

```jsx
const FallbackText = styled(
  Text,
  {
    ...
  },
);
```

## Anatomy

```jsx
export default () => (
  <Avatar.Group>
    <Avatar>
      <Avatar.FallbackText></Avatar.FallbackText>
      <Avatar.Image />
      <Avatar.Badge />
    </Avatar>
  </Avatar.Group>
);
```

## API Reference

### Avatar

It inherits all the properties of React Native's [View](https://reactnative.dev/docs/view#props) component.

### Avatar.Group

It also inherits all the properties of React Native's [View](https://reactnative.dev/docs/view#props) component.

### Avatar.Image

It inherits all the properties of React Native's [Image](https://reactnative.dev/docs/image#props) component.

### Avatar.FallbackText

It inherits all the properties of React Native's [Text](https://reactnative.dev/docs/text#props) component.

### Avatar.Badge

It inherits all the properties of React Native's [View](https://reactnative.dev/docs/view#props) component.

## Example

<AppProvider>
  <CodePreview
    showComponentRenderer={true}
    showArgsController={false}
    metaData={{
      code: ` 
      function App () {
        return (<Wrapper>
          <Avatar.Group>
        <Avatar size="md">
          <Avatar.FallbackText>AB</Avatar.FallbackText>
          <Avatar.Image
            source={{
              uri: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=687&q=80',
            }}
          />
          <Avatar.Badge />
        </Avatar>
      </Avatar.Group></Wrapper>)
    }`,
      scope: { Avatar: AccessibleAvatar, Wrapper },
      argsType: {},
    }}
  />
</AppProvider>
