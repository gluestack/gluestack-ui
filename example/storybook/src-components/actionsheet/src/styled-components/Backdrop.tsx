import { View } from 'react-native';
import { styled } from '@dank-style/react';

export default styled(
  View,
  {
    position: 'absolute',
    left: 0,
    top: 0,
    opacity: 0.3,
    right: 0,
    bottom: 0,
  },
  {}
);
