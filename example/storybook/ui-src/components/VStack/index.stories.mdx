import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="components/Vstack" />

# VStack

"VStack" component is used to create a vertical stack layout for child components in React Native. In React Native, the VStack component can be generated using a third-party accessibility-focused library that uses "styled views" to define the layout, visual design, and behavior of the component.

<Canvas style={{ height: 800 }}>
  <Story id="stories-layout-vstack--v-stack" />
</Canvas>

# Reversed

<Canvas style={{ height: 800 }}>
  <Story id="stories-layout-vstack--v-stack-reversed" />
</Canvas>
