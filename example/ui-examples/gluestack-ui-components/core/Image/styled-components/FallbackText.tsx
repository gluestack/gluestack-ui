import { Text } from 'react-native';
import { styled } from '../../styled';

export default styled(
  Text,
  {
    fontFamily: '$body',
    color: '$blue900',
    bg: '$amber500',
    p: 10,
  },
  {}
);
