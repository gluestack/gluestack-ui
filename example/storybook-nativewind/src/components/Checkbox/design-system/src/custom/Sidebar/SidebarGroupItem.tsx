import { createPressable } from '@gluestack-ui/pressable';
import { StyledSidebarGroupItem } from '../../styled-components';

export const SidebarGroupItem = createPressable({
  Root: StyledSidebarGroupItem,
}) as any;
