import { styled } from '@gluestack-style/react';
import { View } from 'react-native';

export default styled(
  View,
  {
    position: 'absolute',
    borderRadius: 9999,
    zIndex: -1,
  },
  {}
);
