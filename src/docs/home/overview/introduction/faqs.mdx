## FAQs:
<Accordion className="m-5 max-w-[840px] bg-transparent">
      <AccordionItem value="item-1" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    Why Choose gluestack-ui v3?
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
          {/* <AccordionContentText>
            To place an order, simply select the products you want, proceed to
            checkout, provide shipping and payment information, and finalize
            your purchase.
          </AccordionContentText> */}
          <Accordion className="my-5 bg-transparent">
      <AccordionItem value="item-1" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    vs. Traditional UI Libraries
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
Unlike component libraries that force you into their ecosystem:
<ul>
- **Full Customization**: Modify components to fit your exact needs
- **No Bundle Bloat**: Only include components you actually use
- **Universal Compatibility**: Works across React, Next.js, and React Native
</ul>
 </AccordionContent>
</AccordionItem>
<AccordionItem value="item-2" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    vs. Building from Scratch
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
Instead of reinventing the wheel:
<ul>
- **Production-Ready**: Battle-tested components used by thousands of developers
- **Accessibility Built-in**: ARIA compliance and keyboard navigation included
- **Design System**: Consistent design tokens and theming system
- **Community Support**: Active community and regular updates
</ul>
 </AccordionContent>
</AccordionItem>
<AccordionItem value="item-3" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    vs. Platform-Specific Solutions
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
Rather than maintaining separate web and mobile component libraries:
<ul>
- **Single Codebase**: Write once, run everywhere
- **Consistent UX**: Identical behavior across all platforms
- **Shared Knowledge**: Team members can work across platforms
- **Reduced Maintenance**: One component system to maintain
</ul>
 </AccordionContent>
</AccordionItem>
</Accordion>
        </AccordionContent>
      </AccordionItem>
      <AccordionItem value="item-2" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    Getting Started
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
##### Quick Start

###### Step 1: **Initialize a new project**:
   ```bash
   npx create-gluestack@latest my-app
   ```

###### Step 2: **Add components to existing project**:
   ```bash
   npx gluestack-ui init
   npx gluestack-ui add button
   ```

###### Step 3: **Start building**:
   ```tsx
   import { Button, ButtonText } from '@/components/ui/button'
   
   export default function App() {
     return (
       <Button>
         <ButtonText>Get Started</ButtonText>
       </Button>
     )
   }
   ```

##### Supported Frameworks
<ul>
- **Next.js** - Optimized for Next.js 13+ with App Router
- **React Native CLI** - Native mobile development with React Native CLI
- **Expo** - Complete Expo SDK compatibility
- **Monorepos** - Full support for monorepo architectures
</ul>
        </AccordionContent>
        </AccordionItem>
        <AccordionItem value="item-3" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    Why use gluestack-ui v3, when we already have NativeBase?
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
        **gluestack-ui** was originally part of NativeBase, but it has since evolved into a standalone library with significantly improved performance. It prioritizes performance and is optimized for even complex applications with many elements.

We have also shifted from prop-based APIs in NativeBase to compound APIs because that provides a more cohesive, robust and consistent interface for developers.
        </AccordionContent>
        </AccordionItem>
        <AccordionItem value="item-4" className="rounded-lg">
        <AccordionHeader>
          <AccordionTrigger className="focus:web:rounded-lg">
            {({ isExpanded }) => {
              return (
                <>
                  {isExpanded ? (
                    <AccordionIcon as={ChevronUpIcon} className="mr-3" />
                  ) : (
                    <AccordionIcon as={ChevronDownIcon} className="mr-3" />
                  )}
                  <AccordionTitleText>
                    Why use gluestack-ui v3/v2, when we already have gluestack-ui v1?
                  </AccordionTitleText>
                </>
              );
            }}
          </AccordionTrigger>
        </AccordionHeader>
        <AccordionContent className="ml-9">
        <ul>
**gluestack-ui v3/v2** is a major upgrade from **v1**, designed to address the evolving needs of modern developers. With enhanced performance, improved developer experience, and greater flexibility, gluestack-ui v2 offers several advantages over its predecessor:

- **Modular Approach**: Avoid unnecessary bloat by selecting and using only the components you need.
- **Simple Integration**: Easily integrate components by copying and pasting code snippets directly into your project files.
- **Tailwind Class Support**: Enjoy seamless styling with Tailwind's intuitive utility classes with NativeWind.
- **RSC compatible components**: Optimize your applications with components that enhance performance by leveraging React Server Components.
- **Full Customization**: Tailor components to fit your project's needs by adjusting styles and behaviors.
</ul>
        </AccordionContent>
        </AccordionItem>
      </Accordion>