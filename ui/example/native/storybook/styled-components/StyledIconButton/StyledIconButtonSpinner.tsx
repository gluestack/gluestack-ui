import { styled } from '@gluestack/styled';
import { ActivityIndicator } from 'react-native';

export default styled(
  ActivityIndicator,
  {
    baseStyle: {
      style: {
        color: 'white',
      },
    },
  },
  {}
);
