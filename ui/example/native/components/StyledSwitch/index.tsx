import { styled } from '@gluestack/styled';
import { Switch } from 'react-native';

export default styled(
  Switch,
  {
    baseStyle: {
      style: {
        bg: '$primary.500',
      },
    },
  },
  {}
);
