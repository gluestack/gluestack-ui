import {
  StyledBadge,
  StyledBadgeText,
  StyledBadgeIcon,
} from '../../styled-components';
import { createBadge } from '@gluestack/ui-creator';

export const Badge = createBadge({
  StyledBadge,
  StyledBadgeText,
  StyledBadgeIcon,
}) as any;
