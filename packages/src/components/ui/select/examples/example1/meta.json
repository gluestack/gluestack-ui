{
    "argTypes": {
      "variant": {
        "control": {
          "type": "select"
        },
        "options": ["underlined", "outline", "rounded"],
        "defaultValue": "outline"
      },
      "size": {
        "control": {
          "type": "select"
        },
        "options": ["sm", "md", "lg", "xl"],
        "defaultValue": "md"
      }
    },
    "reactLive": {
        "Select": "Select",
        "SelectTrigger": "select",
        "SelectInput": "select",
        "SelectIcon": "select",
        "SelectPortal": "select",
        "SelectBackdrop": "select",
        "SelectContent": "select",
        "SelectDragIndicator": "select",
        "SelectDragIndicatorWrapper": "select",
        "SelectItem": "select",
        "ChevronDownIcon": "icon"
    }
  }
