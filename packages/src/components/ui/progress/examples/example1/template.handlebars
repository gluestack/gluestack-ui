function Example() {
  return (
    <Center className="w-[300px] h-[150px]">
      <Progress value={40} size="{{size}}" orientation="{{orientation}}">
        <ProgressFilledTrack />
      </Progress>
    </Center>
  )
}