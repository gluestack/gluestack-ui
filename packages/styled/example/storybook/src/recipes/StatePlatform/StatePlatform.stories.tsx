import type { ComponentMeta } from '@storybook/react-native';
import { StatePlatform } from './StatePlatform';
const MyStatePlatformMeta: ComponentMeta<typeof StatePlatform> = {
  title: 'recipes/StatePlatform',
  component: StatePlatform,
};

export { StatePlatform } from './StatePlatform';
export default MyStatePlatformMeta;
