import { styled } from '@gluestack/ui-styled';
import { Switch } from 'react-native';

export default styled(
  Switch,
  {
    baseStyle: {
      style: {
        // bg: '$primary.500',
      },
    },
  },
  {}
);
