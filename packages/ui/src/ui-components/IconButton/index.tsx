import {
  StyledIconButton,
  StyledIconButtonText,
  StyledIconButtonSpinner,
} from '../../styled-components';
import { createIconButton } from '@gluestack/ui-creator';

export const IconButton = createIconButton({
  StyledIconButton,
  StyledIconButtonText,
  StyledIconButtonSpinner,
});
