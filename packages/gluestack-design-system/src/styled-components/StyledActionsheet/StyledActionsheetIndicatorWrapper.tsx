import { View } from 'react-native';
import { styled } from 'dank-style';

export default styled(
  View,
  {
    baseStyle: {
      style: {
        py: '$3',
        mt: -4,
        w: '100%',
        alignItems: 'center',
      },
    },
  },
  {}
);
