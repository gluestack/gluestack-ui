import { ScrollView } from 'react-native';
import { styled } from 'dank-style';

export default styled(
  ScrollView,
  {
    baseStyle: {
      style: {
        padding: '$4',
      },
    },
  },
  {}
);
