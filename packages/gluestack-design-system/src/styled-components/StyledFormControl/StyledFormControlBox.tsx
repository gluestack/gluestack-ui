import { View } from 'react-native';
import { styled } from 'dank-style';

export default styled(
  View,
  {
    baseStyle: {
      style: {
        flexDirection: 'column',
        width: '100%',
      },
    },
  },
  {}
);
