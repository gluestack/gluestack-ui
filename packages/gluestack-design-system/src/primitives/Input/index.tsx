import {
  StyledInputRoot,
  StyledInputIcon,
  StyledInput,
} from '../../styled-components';
import { createInput } from '@gluestack/ui-creator';

export const Input = createInput({
  StyledInputRoot,
  StyledInputIcon,
  StyledInput,
}) as any;
