import {
  StyledSelect,
  StyledSelectItem,
  StyledSelectItemList,
  StyledSelectIcon,
} from '../../styled-components';
import { createSelect } from '@gluestack/ui-creator';

export const Select = createSelect({
  StyledSelect,
  StyledSelectItem,
  StyledSelectItemList,
  StyledSelectIcon,
}) as any;
