export interface InterfaceTextProps {
  /**
   *  Renders components as Text children. Accepts a JSX.Element or an array of JSX.Element.
   */
  children?: React.ReactNode | string;
}

export type TextProps = InterfaceTextProps;
